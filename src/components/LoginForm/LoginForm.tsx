import './LoginForm.scss'

import React from 'react'

/*
type PropType= {
  prop1: string;
  prop2: string;
};
*/

// Destructure the props
const LoginForm = (/* { prop1, prop2 }: PropType */): React.ReactElement | null => {
  return <div className="LoginForm"></div>
}

export default LoginForm
