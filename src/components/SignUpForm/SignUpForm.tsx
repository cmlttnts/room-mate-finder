import './SignUpForm.scss'

import React from 'react'
import { Formik } from 'formik'

/*
type PropType= {
  prop1: string;
  prop2: string;
};
*/

// Destructure the props
const SignUpForm = (/* { prop1, prop2 }: PropType */): React.ReactElement | null => {
  return <div className="SignUpForm"></div>
}

export default SignUpForm
